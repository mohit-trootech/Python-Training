"""
built-in module json, which allow us to work with JSON data.
JSON: JSON (JavaScript Object Notation) is a subset of
JavaScript syntax (ECMA-262 3rd edition) used as a lightweight data
interchange format.
"""

import json

print(json.__class__)
print(dir(json))
print(json.__doc__)
